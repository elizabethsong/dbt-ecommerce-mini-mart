
# Transforms raw ecommerce data into analytics-ready staging 
# and mart models, with tests and published documentation.
name: 'ecommerce_mini_mart'
version: '1.0.0'

# This setting configures which "profile" dbt uses for this project.
profile: 'ecommerce_mini_mart'

# These configurations specify where dbt should look for different types of files.
seed-paths: ["seeds"]
model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:         # directories to be removed by `dbt clean`
  - "target"
  - "dbt_packages"


# Configuring models
# Full documentation: https://docs.getdbt.com/docs/configuring-models

# Tell dbt to build all models in the example/
# directory as views. These settings can be overridden in the individual model
# files using the `{{ config(...) }}` macro.
# Overriding final marts to be tables
models:
  ecommerce_mini_mart:
    # Config indicated by + and applies to all files under ecommerce_mini_mart/models/
    staging:
      +materialized: view
    marts:
      +materialized: table
